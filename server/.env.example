# Server environment. Copy to server/.env and fill in real values.
# Neon (Postgres): postgresql://<user>:<password>@<host>.neon.tech/<dbname>?sslmode=require
DATABASE_URL="postgresql://YOURUSER:YOURPASSWORD@YOURHOST.neon.tech/labshare?sslmode=require"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
